# Mermaid Choice Report Preview

Upgrade the Mermaid choice report into a normal scrollable review page with decision stats, wrapping keyboard navigation, fullscreen diagram previews, and submit output that downstream agents can consume. Also update the Mermaid plugin docs so lint failures are fixed and relinted when possible before diagrams are presented.

Use `facts.md` as the shared understanding of accepted behavior.
Use `plan.md` as the approved execution plan.

Done when the choice report template, docs, generated dev/test audit page, and verification checks satisfy every accepted fact in `facts.md`.
