# My Marketplace

When changing plugin or marketplace content, bump the relevant versions according to the official Claude Code and Codex plugin marketplace documentation.

Use this repo's `version-bumper` skill at `plugins/python-dev-workflow/skills/version-bumper/SKILL.md` to update version-bearing files such as `plugin.json`, `marketplace.json`, and package metadata.
