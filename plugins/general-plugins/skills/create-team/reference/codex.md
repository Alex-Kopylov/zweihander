# Codex

Use exact documented tools:
- `Bash` and `apply_patch` for blueprint files.
- `spawn_agent`, `send_input`, `resume_agent`, `wait_agent`, `close_agent` for multi-agent work.
- `spawn_agents_on_csv`; workers must call `report_agent_job_result`.
