# Codex Harness Notes

Use these notes only when the active harness is Codex.

- User decisions: use `request_user_input` when available; otherwise ask in chat.
- Skill invocation: use `$job-hunt-toolkit:prepare-to-send` or load the skill's `SKILL.md`.
