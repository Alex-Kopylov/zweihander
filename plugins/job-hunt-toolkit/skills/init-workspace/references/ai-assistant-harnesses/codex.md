# Codex Harness Notes

Use these notes only when the active harness is Codex.

- Required fields and bounded choices: use `request_user_input` when available; otherwise ask in chat.
- Skill references: preserve `/job-hunt-toolkit:<skill-name>` strings from templates unless the user explicitly asks for different wording.
