# Claude Code Harness Notes

Use this file only when Claude Code is the active assistant harness.

## AskUser

For `AskUserQuestion` prompts in the shared workflow, use Claude Code's available user-question mechanism or plain chat when a structured question is unnecessary.

Ask before installing or updating `@mermaid-js/mermaid-cli`; do not auto-update Mermaid CLI.
