# Codex Harness Notes

Use this file only when the active harness is Codex.

- When the E2E circuit breaker applies, use `request_user_input` when
  available; otherwise ask in chat.
- For the test-first process, use `$dev-workflow:test-driven-development` or
  load the skill's `SKILL.md`.
