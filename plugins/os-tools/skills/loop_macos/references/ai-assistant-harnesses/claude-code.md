# Claude Code Harness Notes

Load this file only when the active assistant harness is Claude Code.

- Syntax: this skill mirrors the built-in `/loop` interval and "every" input syntax.
- User decisions: use `AskUserQuestion` when a structured prompt is useful.
