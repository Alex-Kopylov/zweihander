# Codex Harness Notes

Use this file only when the active harness is Codex.

## Invocation

Skill invocation: use `$work-session-tools:daily` or load the skill's `SKILL.md`.

## Project Context

When source detection or channel discovery needs assistant-specific files, include Codex project instructions such as `AGENTS.md` and Codex settings or MCP configuration exposed in the active environment.

Use Codex MCP tools, apps, or resources that are available in the active tool list for Slack, Microsoft Teams, Telegram, Azure DevOps, and custom MCP sources. If a configured server is not exposed as an available tool, app, or resource, record that source as unavailable and continue.
