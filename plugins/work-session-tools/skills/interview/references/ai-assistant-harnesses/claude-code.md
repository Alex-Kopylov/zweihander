# Claude Code Harness

Use the shared `SKILL.md` workflow directly in Claude Code.

- Use `AskUserQuestion` for each structured item decision.
- Use `Read`, `Edit`, `Write`, and `Bash` when executing agreed actions.
- Keep `preview` content on options when it helps the user compare outcomes.
