# Claude Code Harness Notes

When this skill says to use or suggest the `langfuse-dataset-expert` agent, treat
it as a Claude Code agent and invoke it with the Agent tool when direct
delegation is needed.
