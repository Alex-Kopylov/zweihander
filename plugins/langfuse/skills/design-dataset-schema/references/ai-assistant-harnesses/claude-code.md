# Claude Code Harness Adaptation

Load this file only when the active harness is Claude Code and the workflow needs harness-specific evaluator delegation.

For follow-up LLM-as-Judge evaluator setup, use the Claude Code Agent workflow to delegate to the `langfuse-eval-manager` agent when that agent is available.
