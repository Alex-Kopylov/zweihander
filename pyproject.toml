[project]
name = "ai-agents-marketplace"
version = "0.1.0"
description = "Personal plugin marketplace for AI agents."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pydantic[email]>=2.13.5",
    "pytest>=9.0.3",
]
