[project]
name = "ai-agents-marketplace"
version = "0.1.0"
description = "Personal plugin marketplace for AI agents."
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.1",
    "pathspec>=0.12",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
